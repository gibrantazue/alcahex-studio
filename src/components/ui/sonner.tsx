// Simple toast component without external dependencies
"use client";

import { useTheme } from "next-themes";

const Toaster = () => {
  return null; // Removed toast functionality for simple landing page
};

export { Toaster };
